// Baked in by the vite config: "" when served (assets are proxied to the local
// alpineview_api, so same-origin), the deployed API's URL for a production build.
export const API_BASE_URL = __API_BASE_URL__;
